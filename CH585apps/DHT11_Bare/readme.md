# DHT11温湿度程序

读取DHT11温湿度并通过串口打印

串口打印四个uint8数据（并非字符），第一个为湿度数据（百分比），第二个固定为0，第三个为温度整数部分，第四个为温度小数部分

## 接线
DHT11的VCC和GND接单片机VCC和GND，DATA接PB0。DATA可在DHT11.h中更改引脚号，但如果要切换到GPIOA记得把下面的拉高拉低等函数都一起改了。

USB转串口的RXD接PB7

## 注意事项

DHT11在上电后的1秒内是不稳定的，一秒之后才能进行操作。程序内没有对这个特性进行处理。





